# Pico 皮可 · 产品需求文档

版本 1.0.0 · 2026-09

> Pico 的无障碍实时翻译架构演化自 [krisir/floattrans](https://github.com/krisir/floattrans) v0.2.0（MIT），感谢原作者。

## 1. 产品定位

Pico（皮可）是一只住在 macOS 菜单栏里的实时翻译小助手：在任意支持无障碍（Accessibility）的输入框中打字，译文以低打扰浮窗实时浮现；对不暴露无障碍接口的应用（如微信），提供剪贴板翻译作为等效通路。默认使用系统本地翻译，内容不出设备。

## 2. 核心场景

- **场景 A · 输入实时翻译**：用户在任意支持无障碍的输入框中输入源语言文字，停止输入片刻后，译文浮窗自动出现在不遮挡输入框的位置
- **场景 B · 剪贴板翻译**：在任意应用中复制文字，按快捷键（或自动监听模式复制即触发），译文浮窗弹出——覆盖微信等无障碍受限应用

## 3. 功能需求

### FR-1 输入实时翻译
- 无障碍监听前台应用焦点文本框，按句子识别，同一句动态更新
- 防抖三档：快 300ms / 均衡 450ms / 慢 700ms；支持整句翻译与快捷键触发（默认 ⌃⇧T）
- 源语言与目标语言独立选择：中 / 英 / 日 / 俄 / 韩 / 法 / 德 / 西
- 译文可写回输入框（默认 ⌥⇧[）或复制到剪贴板（默认 ⌥⇧]），快捷键可自定义
- 密码输入框（安全文本框）永不读取；支持排除指定应用

### FR-2 剪贴板翻译
- 快捷键触发（默认 ⌥⇧V）：翻译当前剪贴板文本
- 自动监听模式（可选）：剪贴板变化即翻译，仅处理包含源语言的内容，长度上限 2000 字符
- 应用自身复制译文时抑制回环触发

### FR-3 悬浮窗
- 整卡拖动移动，拖过的位置自动记忆，后续译文跟随
- 自动避开焦点输入框区域，不遮挡正在输入的内容
- 透明度 30%–100% 实时调节；六种主题色；四种背景效果（毛玻璃 / 轻透玻璃 / 厚实 / 纯色）
- 最多同时 3 个浮窗；替换或堆叠显示；支持右上 / 底部居中 / 右下；边距、字号、自动隐藏（5–60 秒或永不）可调
- 右上角一键复制译文；每个浮窗可手动关闭

### FR-4 翻译引擎
- **本地翻译**：macOS 系统 Translation Framework，内容不出设备；语言包缺失时应用内引导下载
- **大语言模型 API**：OpenAI 兼容接口、Claude、DeepSeek、GLM、自定义地址；提示词与思考模式可配；多模型按序自动故障切换，切换阈值可配
- API 密钥仅存本机钥匙串（禁止 iCloud 同步），不写入偏好设置

### FR-5 翻译历史
- 本机 SQLite 存储：原文、译文、语言方向、时间
- 保留期限：1 天 / 7 天 / 30 天 / 6 个月 / 永久，过期自动清理
- 按日期浏览；导出 Markdown / Excel；一键清空（VACUUM 无残留）

### FR-6 朗读译文
- 时机多选（停止输入 / 整句 / 快捷键）；自动使用目标语言的系统语音

### FR-7 设置界面
- 侧边栏导航（彩色图标 + 分组卡片），macOS 系统设置风格
- 简体中文 / English 双语界面，切换即时生效

### FR-8 其它
- 登录时启动；Dock 图标（点击开设置、右键菜单暂停 / 退出）
- 检查更新（对照 GitHub Releases）；中英双语朗读语音随目标语言切换

## 4. 非功能需求

- **隐私**：默认本地翻译内容不出设备；无遥测、无统计、无崩溃上报；零第三方依赖
- **性能**：本地翻译延迟百毫秒级；剪贴板检测 0.25 秒轮询；翻译缓存有上限
- **稳定**：多模型自动故障切换；历史坏数据容错；诊断日志默认关闭、仅记录文本长度、自动限容

## 5. 平台与分发

macOS 15+ · Apple Silicon · DMG 分发 · 本地自签名证书构建（正式对外分发建议 Developer ID 公证）
